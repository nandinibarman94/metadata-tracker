services:
  api:
    build: .
    ports:
      - "8000:8080"
    command: >
      sh -c "alembic upgrade head && uvicorn src.main:app --host 0.0.0.0 --port 8080"
    environment:
      - DB_FILE=/data/MetadataTracker.db
    volumes:
      - ./db:/data

volumes:
  sqlite_data: